# Test requirements for claude-self-reflect unified state tests
# Install with: pip install -r tests/requirements-test.txt

# Core testing framework
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-timeout>=2.1.0
pytest-xdist>=3.0.0  # For parallel test execution
pytest-benchmark>=4.0.0  # For performance benchmarking

# Test utilities
psutil>=5.9.0  # For memory/CPU monitoring in performance tests
mock>=4.0.0  # Additional mocking utilities
freezegun>=1.2.0  # For time-based testing
factory-boy>=3.2.0  # For test data generation

# Coverage reporting (optional)
pytest-cov>=4.0.0
coverage>=7.0.0

# Code quality checks (optional)
pytest-flake8>=1.1.0
pytest-mypy>=0.10.0

# Async testing support
pytest-asyncio>=0.21.0

# File system testing utilities
pyfakefs>=5.0.0  # For filesystem mocking

# Concurrency testing
pytest-forked>=1.4.0  # For forked process testing

# Platform-specific dependencies
filelock>=3.9.0  # Cross-platform file locking
portalocker>=2.6.0  # Alternative file locking

# Development dependencies
ipdb>=0.13.0  # Interactive debugging
rich>=13.0.0  # Rich terminal output for debugging